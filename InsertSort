# Функция для сортировки Insert:

A = [77, 64, 34, 25, 5, 12, 22, 6, 90]


def InsertSort(A):
    # по псевдокоду
    for i in range(1, len(A)):
        t = A[i]
        j = i
        while j > 0 and A[j - 1] > t:
            A[j] = A[j - 1]
            j -= 1
        A[j] = t



print("Sorted array is:")
for i in range(1, len(A)):
 print("% d" % A[i])
