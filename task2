# ЛАБОРАТОРНАЯ РАБОТА 1
# Вариант 15
# Определить есть ли среди заданных целых чисел A, B, C, D хотя бы одно чётное.

while True:
    print("Введите 4 целых числа: ")
    a, b, c, d = int(1), int(2), int(3), int(4)

    if (a % 2 == 1) or (b % 2 == 1) or (c % 2 == 1) or (d % 2 == 1):
        print("среди  целых чисел ЕСТЬ нечетное число")
    else:
        print("среди  целых чисел НЕТ нечетного числа")
    if input(' Continue? ') != 'yes':
        break
